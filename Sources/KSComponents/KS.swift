import SwiftUI

/// KS namespace for all design system components
public enum KS {
    /// Animation presets namespace
    public enum Animation {}

    /// Color definitions namespace
    public enum Colors {
        /// Tertiary text color for subtle UI elements
        public static let textTertiary = Color.gray.opacity(0.6)
    }
}
